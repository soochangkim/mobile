@model SKBusService.Models.RouteStop
@using Microsoft.AspNetCore.Http

@{
    ViewData["Title"] = "Create for " + Context.Session.GetString("busRouteCode") + "-" + Context.Session.GetString("routeName");
}

<h2>@ViewData["Title"]</h2>

<form asp-action="Create" asp-route-BusRouteCode="@ViewData["BusRoutecode"]">
    <div class="form-horizontal">
        <h4>RouteStop</h4>
        <hr />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group">
            <label asp-for="BusStopNumber" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <select asp-for="BusStopNumber" class ="form-control" asp-items="ViewBag.BusStopNumber"></select>
            </div>
        </div>
        <div class="form-group">
            <label asp-for="OffsetMinutes" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <input asp-for="OffsetMinutes" class="form-control" />
                <span asp-validation-for="OffsetMinutes" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
