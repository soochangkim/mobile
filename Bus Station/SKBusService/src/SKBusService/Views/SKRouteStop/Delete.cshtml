@model SKBusService.Models.RouteStop
@using Microsoft.AspNetCore.Http

@{
    ViewData["Title"] = "Delete for " + Context.Session.GetString("busRouteCode") + "-" + Context.Session.GetString("routeName");
}

<h2>@ViewData["Title"]</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>RouteStop</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.OffsetMinutes)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.OffsetMinutes)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            <a asp-action="Index">Back to List</a>
        </div>
    </form>
</div>
